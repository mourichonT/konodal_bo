// Constante injectée par Vite au moment de la compilation (cf. `define` dans
// vite.config.ts) - pas une variable d'environnement, elle n'existe pas à
// l'exécution.
declare const __DEV_SERVER__: boolean
