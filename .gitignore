# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Config Firebase (pas un vrai secret, mais garde .env hors du repo par
# hygiène/portabilité - cf. .env.example pour le format attendu).
# .env.development/.env.production : valeurs par environnement (konodal-dev/
# konodal-prod), chargées automatiquement par Vite selon la commande
# (`vite` = development, `vite build` = production) - cf. .env.example.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
